spring.application.name=ecommerce 

# Database Configuration - Using Railway Environment Variables
spring.datasource.url=jdbc:mysql://${MYSQLHOST:mysql.railway.internal}:${MYSQLPORT:3306}/${MYSQLDATABASE:railway}?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mouryagauravecommerce@gmail.com
spring.mail.password=fjrc uxzg bkta ujns
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Devtools and static resources - auto reload
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.web.resources.cache.period=0
spring.web.resources.chain.cache=false

# Admin bootstrap
app.admin.email=mouryagauravecommerce@gmail.com
app.admin.name=Admin
app.admin.create-if-missing=true

# File upload size
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Server Port (Railway will inject PORT variable)
server.port=${PORT:8080}

MAIL_USERNAME=mouryagauravecommerce@gmail.com
MAIL_PASSWORD=fjrc uxzg bkta ujns